#cv
  .cv-section.fixed.first-cv-section
    .person
      = image_tag "photo.png", :width => "100", :height => "100"
      %h1 Radu Vlad
      %h2 Rails Consultant

    = render "contact_info"

  = render "summary"
  = render "technical_skills"
  = render "work_experience"
  = render "education"


